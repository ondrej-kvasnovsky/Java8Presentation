// 1.7 code
interface Process {
    List list(String data);

    Stream stream(String data);
}

Process process = new Process() {
    public List list(String data) {
        return Arrays.asList(data.split(" "));
    }
};

List processed = process.list("a b c");

System.out.println(processed);